---
file-bundles:
  files:
    - "[[./Trip assets/Route.svg]]"
  folders:
    - ./Trip assets
---

# Trip note

An ordinary note that declares a bundle. The `file-bundles` key in its frontmatter names one file and one folder that belong with it, so moving, renaming or deleting this note carries them along.
